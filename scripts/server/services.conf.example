# ============================================================================
# Server Services Configuration
# ============================================================================
#
# This file defines which services to manage and how to control them.
#
# Each service can have the following properties:
#   - <service>_process: Process name for pgrep (required)
#   - <service>_start_command: Command to start the service (required)
#   - <service>_stop_command: Custom stop command (optional)
#   - <service>_check_command: Custom check command (optional)
#   - <service>_required_command: Command to verify before starting (optional)
#   - <service>_skip_on_normal_stop: Skip during normal stop (optional)
#

# List of services to manage (space-separated)
SERVICES="nginx php-fpm cloudflared battery sshd"

# ============================================================================
# NGINX Configuration
# ============================================================================
nginx_process="nginx"
nginx_start_command="nginx"
nginx_required_command="nginx"

# ============================================================================
# PHP-FPM Configuration
# ============================================================================
php_fpm_process="php-fpm"
php_fpm_start_command="php-fpm"
php_fpm_required_command="php-fpm"

# ============================================================================
# Cloudflared Configuration
# ============================================================================
cloudflared_process="cloudflared"
cloudflared_start_command="termux-chroot cloudflared tunnel run my-tunnel"
cloudflared_required_command="cloudflared"

# ============================================================================
# Battery Monitor Configuration
# ============================================================================
battery_process="php /data/data/com.termux/files/home/battery.php"
battery_start_command="nohup php /data/data/com.termux/files/home/battery.php"
battery_required_command="php"

# ============================================================================
# SSHD Configuration
# ============================================================================
sshd_process="sshd"
sshd_start_command="sshd"
sshd_required_command="sshd"
sshd_skip_on_normal_stop="true"
